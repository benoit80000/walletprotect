'use client';
import React,{useEffect,useState}from'react';
export default function ThemeToggle(){const[theme,setTheme]=useState<'dark'|'light'|'blue'>('dark');useEffect(()=>{const s=typeof window!=='undefined'?localStorage.getItem('ws-theme'):null;const t=(s==='dark'||s==='light'||s==='blue')?s as any:'dark';setTheme(t);document.documentElement.className=t;},[]);function cycle(){setTheme(p=>{const n=p==='dark'?'light':p==='light'?'blue':'dark';if(typeof window!=='undefined'){localStorage.setItem('ws-theme',n);document.documentElement.className=n;}return n;});}return <button className="btn btn-outline" onClick={cycle}>Theme: {theme}</button>}