'use client';
import React from'react';
export default function PresetsPanel({onApply}:{onApply:(preset:'hodl'|'farmer'|'power'|'nft')=>void}){const items=[{key:'hodl',label:'HODL Max',desc:'Lock transfers (limit = 0)'},{key:'farmer',label:'Prudent Farmer',desc:'Low limits + whitelist your dApp'},{key:'power',label:'Power User',desc:'Higher daily limits'},{key:'nft',label:'NFT Vault',desc:'Strict whitelist, default lock'},] as const;return(<div className="card"><b>Presets</b><div className="help" style={{marginTop:8}}>Choose a profile to auto-fill sensible limits. You can tweak values before applying.</div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:8,marginTop:8}}>{items.map(it=>(<div key={it.key} style={{border:'1px solid rgba(255,255,255,0.15)',borderRadius:12,padding:12}}><div style={{fontWeight:600}}>{it.label}</div><div style={{opacity:.7,fontSize:12,margin:'6px 0 10px'}}>{it.desc}</div><button className="btn" onClick={()=>onApply(it.key as any)}>Apply</button></div>))}</div></div>)}