'use client';
import React,{useState}from'react';
import PresetsPanel from'../(components)/PresetsPanel';
import SpendPermissions from'../(components)/SpendPermissions';
import RiskBanner from'../(components)/RiskBanner';
export default function BasePage(){const[protect,setProtect]=useState(true);const[demo,setDemo]=useState(false);const initial=[{symbol:'USDC',token:'0x8335...2913',spender:'0xy0urApp',allowance:'50000000',period:'86400'},{symbol:'WETH',token:'0x4200...0006',spender:'0xy0urApp',allowance:'200000000000000000',period:'86400'},];function applyPreset(p:string){alert('Preset '+p+' applied');}return(<main><div style={{display:'flex',gap:8,alignItems:'center',marginBottom:12}}><span className="badge">Security Score: 86</span><button onClick={()=>setProtect(!protect)} className="btn btn-outline">{protect?'Protect ON (Private RPC)':'Protect OFF'}</button><button onClick={()=>setDemo(!demo)} className="btn btn-outline">{demo?'Demo Mode ON':'Demo Mode OFF'}</button></div><div className="help" style={{marginBottom:12}}>Protect ON sends sensitive transactions via a private relay to reduce frontrunning risk. Demo Mode simulates without sending.</div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12}}><PresetsPanel onApply={applyPreset as any}/><div className="card"><b>Guardian & Delay</b><div className="help" style={{marginTop:8}}>Add a recovery guardian and a delay for large transfers (coming soon).</div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:8,marginTop:8}}><input placeholder="Guardian address (0x…)"/><input placeholder="Delay (seconds)"/></div><div style={{marginTop:8}}><button className="btn">Apply</button></div></div></div><div style={{marginTop:12}}><SpendPermissions initial={initial as any}/></div><div style={{marginTop:12}}><RiskBanner/></div><div style={{display:'flex',justifyContent:'end',marginTop:12}}><button className="btn">Execute with Protect RPC</button></div></main>)}